{
  "name": "slop-filter",
  "version": "0.8.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "try": "node scripts/try.mjs",
    "fit": "node scripts/fit.mjs",
    "test": "node --test",
    "lint:firefox": "node scripts/zip.mjs firefox && rm -rf /tmp/slop-filter-firefox-lint && mkdir /tmp/slop-filter-firefox-lint && unzip -q dist/slop-filter-*-firefox.zip -d /tmp/slop-filter-firefox-lint && npx --yes web-ext@8.9.0 lint --source-dir /tmp/slop-filter-firefox-lint",
    "test:firefox": "node scripts/firefox-smoke.mjs"
  }
}
